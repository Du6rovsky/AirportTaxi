import {IMainConfig} from '@/core/interfaces/IMainConfig.interface';

export const CONFIG: IMainConfig = {
  // http://jsonplaceholder.typicode.com/guide/
  api: "https://jsonplaceholder.typicode.com"
};
